build the ingest.py and push to Qdrant build a quality vector DB at production level. Embeddings and ingestion at phase 1 
chunk to -> Embeddings -> vectors -> Vector DB 
The vectorDB should be persistent in the cloud so that it can be access 
via APIs 
do the best practices not sure about this :  [During ingestion, store more than just the vector,stable chunk IDso future updates don't create duplicates.]
---

───────────────
